<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.Mybatis.mapper.UserMapper">

    <!--  int insertUser();  -->
    <insert id="insertUser">
        insert into tb_user
        values (null, 'admin', '123', '18', '女', '123@qq.com')
    </insert>

    <!--int updateUser();-->
    <update id="updateUser">
        update tb_user set username = 'root', password = '521' where id = 4
    </update>

    <!--int deleteUser();-->
    <delete id="deleteUser">
        delete from tb_user where id = 4
    </delete>

    <!--User selectUserById();-->
    <select id="selectUserById" resultType="com.Mybatis.pojo.User">
        select * from tb_user where id = #{id}
    </select>

    <!--List<User> selectUserList();-->
    <select id="selectUserList" resultType="User">
        select * from tb_user
    </select>

    <!--int selectUserCount();-->
    <select id="selectUserCount" resultType="Int">
        select count(*) from tb_user
    </select>

    <!--Map<String, Objects> selectUserToMap(@Param("id") int id);-->
    <select id="selectUserToMap" resultType="Map">
        select * from tb_user where id = #{id}
    </select>

    <!--List<Map<String, Objects>> selectUserToList(@Param("id") int id);-->
    <select id="selectUserToList" resultType="Map">
        select * from tb_user
    </select>

    <!--Map<String, Object> selectUserToMap();-->
    <select id="selectUserToMaps" resultType="Map">
        select * from tb_user
    </select>

</mapper>